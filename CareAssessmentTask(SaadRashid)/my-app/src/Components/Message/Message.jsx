import React from "react";
import "./Message.css";

const Message = (props) => (
    <div className="message_container">
        <p className="message_text">Tap the card to increment the count</p>
    </div>
)

export default Message;